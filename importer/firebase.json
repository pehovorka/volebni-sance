{
  "functions": {
    "predeploy": ["cd functions && yarn && yarn --cwd \"$RESOURCE_DIR\" build"],
    "source": "functions"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
